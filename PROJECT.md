# Проект «О & Д» (vmeste) — полная справка

Личный сайт-трекер для пары (Дима и Оля): привычки, ачивки, счётчик «вместе»,
письма-капсулы, общие фото и музыка. Синхронизация между устройствами через Firebase.
Дизайн — **аниме-киберпанк** (неон, HUD, сканлайны, глитч, дата-дождь).

_Последнее обновление справки: 2026-06-02 (v7)._

> 🔄 **Правило поддержки:** при каждом деплое на GitHub обновлять этот файл —
> дату «последнее обновление», список функций/данных при изменениях и раздел
> «История ключевых коммитов». Обновлять и локальную копию (`Downloads\vmeste_PROJECT.md`),
> и копию в репозитории (`PROJECT.md`).

---

## 1. Ссылки и доступы

| Что | Значение |
|---|---|
| GitHub репозиторий | https://github.com/impusha3-ship-it/vmeste |
| Живой сайт (GitHub Pages) | https://impusha3-ship-it.github.io/vmeste/ |
| Локальный рабочий файл | `C:\Users\impus\Downloads\index.html` |
| Главный файл в репо | `index.html` (одностраничное приложение, всё в одном файле) |
| Пароль входа на сайт | `2031` (захардкожен в JS, переменная `PWD`) |

> Пароль — чисто визуальная заглушка на клиенте, базу он НЕ защищает.

---

## 2. Firebase (синхронизация данных)

- Realtime Database, проект **vmeste-a8b90**.
- Все данные лежат под узлом **`/app`**, каждый ключ — JSON-строка.
- Конфиг (уже вшит в `index.html`, блок `<script type="module">` в конце):

```js
const firebaseConfig = {
  apiKey: "AIzaSyBYnVPyST46NpCnyLNmIkxfhJfJclFIsTs",
  authDomain: "vmeste-a8b90.firebaseapp.com",
  databaseURL: "https://vmeste-a8b90-default-rtdb.firebaseio.com",
  projectId: "vmeste-a8b90",
  storageBucket: "vmeste-a8b90.firebasestorage.app",
  messagingSenderId: "539477698753",
  appId: "1:539477698753:web:c720409e97782bf6804c40",
  measurementId: "G-Q2CR9K65P2"
};
```

- **Правила базы** (Firebase Console → Realtime Database → Rules), без них запись не работает:

```json
{ "rules": { "app": { ".read": true, ".write": true } } }
```

> ⚠️ База публичная: кто знает адрес — может читать/писать. Для закрытия нужен Firebase Auth
> или общий ключ в правилах (пока не сделано).

---

## 3. Технологии

- Чистый HTML + CSS + JS в одном файле `index.html` (без сборки, без фреймворков).
- Firebase Realtime Database (модульный SDK 10.12 через CDN gstatic).
- YouTube IFrame API (аудио мини-плеер).
- Web Audio API (звуки набора/бип/тики).
- Шрифты Google: **Tektur** (заголовки), **Jura** (текст), **JetBrains Mono** (цифры/метки) — все с кириллицей.

---

## 4. Хранение данных (ключи в localStorage + Firebase /app)

Слой `ld(key, default)` / `sv(key, value)` читает из памяти `MEM` (зеркало localStorage),
пишет в localStorage + Firebase. `window.__applyRemote` принимает обновления из облака.

| Ключ | Содержимое |
|---|---|
| `c_ГГГГ_М_Д` | отметки дня: `{d:bool, o:bool}` (d=Дима/зарядка, o=Оля/таблетки). Месяц 0–11. |
| `ach` | объект `{achId: true}` — выполненные ачивки |
| `customAchs` | массив пользовательских ачивок `{id:'c..', title, cat, svg}` |
| `together_since` | дата начала отношений (строка `YYYY-MM-DD`) |
| `events` | массив важных дат `{id, title, date}` (будущие + прошедшие) |
| `letters` | письма-капсулы `{id, from, unlock, text, cover, created}` |
| `photos` | фото `{id, data(base64 jpeg ~1000px), by, cap, ts}` |
| `tracks` | музыка `{id, url, title, by, ts}` |
| `questions` | вопрос дня: `{dateKey:{d:'',o:''}}`, dateKey=`Y-M-D` |
| `q_who` | (устар.) кто отвечает — заменён на `me` |
| `me` | кто вошёл: 'd'/'o' (ЛОКАЛЬНО). Подставляется во все «кто добавил» |
| `pet` | питомец Амням: `{xp, claimed:{ключи событий}}` |
| `tierlists` | тирлисты: `{weeks:{weekKey:{themeId,labels[],place:{d:{},o:{}}}}, votes:{weekKey:{cands[],d:{},o:{}}}}`. weekKey=понедельник `Y-M-D`, place — `{tN:tierIdx}` по индексу элемента |
| `lastBackup` | время последнего бэкапа (ЛОКАЛЬНО, напоминание только Диме) |
| `muted` | звук выкл (`'1'`) — ЛОКАЛЬНО на устройстве, не синхронизируется |

---

## 5. Функциональность (вкладки)

1. **Календарь** — две привычки (Дима: зарядка, Оля: таблетки), отметки по дням, стрики.
   - Стрик: считается по вчерашний день; сегодня необязательно; обнуляется только на след. день после пропуска.
2. **Вместе** — живой счётчик дней/часов/минут/секунд с даты `together_since`; вехи (недели, годы, до 100 дней, годовщина); раздел «Важные даты» (будущие «через N дней» и прошедшие «N дней назад»).
3. **Письма** — капсулы времени: текст + дата открытия + обложка (5 узорных стилей). До даты запечатано.
4. **Фото** — загрузка с авто-сжатием на клиенте (~1000px JPEG), подпись, автор, лайтбокс.
5. **Музыка** — плейлист; YouTube → аудио мини-плеер (скрытое видео, low quality, экономия трафика); прочие ссылки — кнопкой.
6. **Ачивки** — 2 раздела («Поцелуи» / «Остальное»), прогресс-бары, золотые SVG-иконки в неоновых HUD-рамках, добавление своих, авто-подгон размера текста.
7. **Статистика** — стрики, выполнение за 30 дней, кол-во ачивок, бары.
8. **Тирлисты** — еженедельный тирлист (тиры S/A/B/C/D, названия редактируются). Раскладка тапом (элемент → тир). Дима и Оля раскладывают независимо; раскладка второго под замком, пока сам не разложишь все элементы. За готовый тирлист +30 XP питомцу. Голосование за тему следующей недели (5 случайных тем, галочки от обоих): в понедельник авто-выбор случайной из отмеченных обоими, иначе из отмеченных хоть кем-то, иначе новая случайная. Выбор детерминированный (сид по неделе → совпадает на обоих устройствах). 40 тем (~16–24 элемента); первая тема — Фрукты. Архив прошлых недель (обе раскладки рядом).

Прочее: звук набора + бип доступа (кнопка 🔊/🔇 в шапке), конфетти, дешифровка цифр при переходе вкладок.

---

## 6. Дизайн (аниме-киберпанк)

- Палитра: void `#06070d`, циан `#00f0ff`, магента `#ff2a6d` (Дима), зелёный `#05ffa1` (Оля).
- Эффекты: неоновая сетка-перспектива, сканлайны+развёртка, виньетка, дата-дождь (canvas), неоновый курсор-трейл, глитч (логотип + ховер карточек), HUD-углы (clip-path), свечение.
- Вход = «экран взлома»: печатающийся boot-лог + ACCESS GRANTED/DENIED.
- Стиль реализован как слой-надстройка `<style>` + 2 `<script>` в конце файла (после основного кода) — легко откатывается.
- Базовый «тёплый кремово-золотой» дизайн всё ещё под низом (первый `<style>`), киберпанк его перекрывает через `!important`.

---

## 7. Как редактировать и публиковать

Работаем с `C:\Users\impus\Downloads\index.html`. Для деплоя нужен клон репозитория
(каталог во временной папке может удаляться — при необходимости клонировать заново):

```powershell
# (1) однократно — клонировать, если клона нет
git clone https://github.com/impusha3-ship-it/vmeste.git "$env:TEMP\vmeste"

# (2) каждый деплой — скопировать файл, закоммитить, запушить
Copy-Item "C:\Users\impus\Downloads\index.html" "$env:TEMP\vmeste\index.html" -Force
git -C "$env:TEMP\vmeste" add -A
git -C "$env:TEMP\vmeste" commit -m "описание изменений"
git -C "$env:TEMP\vmeste" push origin main
```

GitHub Pages обновляется ~через минуту. На устройствах сбрасывать кэш: **Ctrl+F5**.

Откат к предыдущей версии: `git -C "$env:TEMP\vmeste" revert --no-edit <хэш> ; git ... push`.

---

## 8. Известные ограничения / на будущее

- Фото в Realtime DB (base64) — ок для пары; при ОЧЕНЬ большом объёме переводить на Firebase Storage.
- YouTube «аудио-режим» экономит трафик частично (видеопоток всё равно качается в low-res). Настоящее аудио — Spotify/SoundCloud (плееры можно добавить).
- База открыта на чтение/запись — при желании добавить защиту.
- Не сделано из идей: настроение дня, бэкап/экспорт данных в файл, спец-ачивка за 30-дневный общий стрик, авто-название трека, плееры Spotify/SoundCloud, перерисовка иконок под киберпанк-сюжеты.

### Очередь задач
- [x] Убрать поле «Категория» в модалке ачивки — новые попадают в «Остальное».
- [x] Удалить встроенную ачивку «Поцеловаться в лифте» (id `a17`).
- [x] При открытии фото скрывать киберпанк-фильтр (сканлайны/сетку/виньетку) — чистое фото в лайтбоксе (`body.lightbox-open`).
- [x] В письмах — удаление прямо с карточек (крестик при наведении / всегда на мобиле), работает и для запечатанных.
- [x] Категория ачивки — select из 2 вариантов (Поцелуи / Остальное).
- [x] Иконки ачивок → набор Lucide (вариант B, MIT).
- [x] Установлены скиллы: superpowers (14 шт), caveman (7 шт, режим экономии токенов).
- [x] Раздел «Тирлисты»: еженедельный тирлист с тап-раскладкой, редактируемые названия тиров, замок на раскладке партнёра до своей готовности, голосование за тему следующей недели (детерминированный авто-выбор в понедельник), 40 тем, архив, +30 XP питомцу. Ключ `tierlists`.
- [x] Спец-ачивка a29 «30 дней стрик» — авто-разблок при обоих стриках ≥30, эффект SYNC COMPLETE (конфетти+звук+вспышка), ручной клик отключён, раздел «Остальное».

---

## 9. История ключевых коммитов (main)

- Firebase realtime sync
- Богатый дизайн + разделы «Вместе» и «Письма»
- Детальные иконки, добавление своих ачивок, 2 раздела
- Прогресс-бары ачивок, звуки, обложки писем
- Полный редизайн в аниме-киберпанк (неон, HUD, экран взлома)
- Киберпанк-эффекты: дешифровка цифр, глитч, дата-дождь, курсор-трейл, звуки
- Разделы Фото и Музыка
- YouTube аудио мини-плеер, HUD-рамки иконок, авто-подгон текста
- Логика стрика по вчерашнему дню
- Категория ачивки (2 варианта), удаление письма «Поцеловаться в лифте», чистый фото-лайтбокс, удаление писем
- Иконки ачивок → Lucide (вариант B)
- Спец-ачивка 30-дневного стрика (SYNC COMPLETE auto-unlock)
- Раздел «Вопрос дня» (139 уникальных вопросов, ответы обоих, открытие после обоих, архив; ключ `questions`)
- Декор v3: glitch-cut переход вкладок, нижний neon status-bar (часы+стрики), боковые метки, полировка (focus-ring, tap-feedback, отступы) — коммит `9ad83cf`, откат `git revert 9ad83cf`
- Выбор «кто вошёл» (Дима/Оля) на экране входа → ключ `me`; подставляется во все «кто добавил» (фото/музыка/письма/вопрос)
- Бэкап: кнопка 💾 в шапке — экспорт/импорт всех данных в .json; напоминание (пульс) только Диме раз в 7 дней
- Неоновые киберпанк-конверты писем (5 тем: циан/магента/зелёный/золото/ночь)
- Музыка не прерывается при смене вкладок (постоянные YT-хосты вне списка)
- **Питомец «Амням»** (вкладка «Питомец»): XP за действия, 18 уровней / 6 стадий, эволюция SVG (форма/черты/кибер-декор/цвет/настроение), backfill за прошлую активность, level-up fx; ключ `pet`. XP: день/таблетка +10, оба +10, вопрос +15, фото +20, трек +15, письмо +25, ачивка +50. Анти-чит через `pet.claimed`. `petGain(key,xp)` / `petLose(key,xp)`.
- Амням теряет XP при снятии галки дня/ачивки и при удалении фото/трека/письма (`petLose`)
- **Раздел «Тирлисты»**: еженедельный тирлист (тиры S/A/B/C/D, редактируемые названия), тап-раскладка, раскладка партнёра под замком до своей готовности, +30 XP питомцу за готовый тирлист; голосование за тему след. недели (5 тем, галочки обоих, детерминированный авто-выбор в понедельник по сиду недели); 40 тем (первая — Фрукты), архив. Ключ `tierlists`
